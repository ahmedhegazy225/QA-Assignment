{
  "name": "playwright-framework",
  "version": "1.0.0",
  "description": "A Playwright framework for automated UI and API testing.",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "allure:report": "npx playwright show-report"
  },
  "dependencies": {
    "@playwright/test": "^1.36.0",
    "allure-playwright": "^2.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
